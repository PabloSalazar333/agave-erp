server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # It is the server, so it doesn't register with itself
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0
